[mcp_servers.dettivo]
command = "/usr/bin/dettivo"
args = ["mcp", "serve"]

[mcp_servers.dettivo.env]
DETTIVO_IPC_TOKEN = "<set-me>"

Next step: merge this into $HOME/.codex/config.toml, or run again with --write.
