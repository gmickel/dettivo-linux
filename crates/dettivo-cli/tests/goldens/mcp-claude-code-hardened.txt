claude mcp add --scope user --transport stdio dettivo --env 'DETTIVO_IPC_TOKEN=<set-me>' -- /usr/bin/dettivo mcp serve

Next step: run this line, or `--write` for a project .mcp.json in the current directory.
