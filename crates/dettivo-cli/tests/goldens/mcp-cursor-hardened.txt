{
  "mcpServers": {
    "dettivo": {
      "args": [
        "mcp",
        "serve"
      ],
      "command": "/usr/bin/dettivo",
      "env": {
        "DETTIVO_IPC_TOKEN": "<set-me>"
      },
      "type": "stdio"
    }
  }
}

Next step: merge this into $HOME/.cursor/mcp.json, or run again with --write.
