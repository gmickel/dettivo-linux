---
satisfies: [R1, R2, R3, R4, R5, R6]
---
# fn-21-first-run-keys-models-try-it.1 Implement fn-21: the first-run flow (keys, models, try it), its daemon additions, drives and baselines

## Description
TBD

## Acceptance
Every R-ID in the parent spec's ## Acceptance Criteria is satisfied; judge this task against the spec's criteria directly.

## Done summary
### Summary
Built first run. The app's `onboarding` route is now the three designed screens (qt/qml/Dettivo/app/OnboardingRoute.qml with `KeysStep`, `ModelsStep`, `TryItStep`, `BindingRow`, `ModelRow`, `FirstRunFooter`; a `DettivoStyle/TextArea` joins the style) over two host models (qt/host/app/first_run_model.{h,cpp}, first_run_models.{h,cpp}): `FirstRunModel` decides on every start until a completion is recorded whether first run is needed (a speech model `ready` or `unverified` from `speech.models.status`, VAD models excluded, and either bound actions from `hotkeys.status` or a sourced snippet from `hotkeys.setup { write: false }`), writes `first_run.completed_at` at once when the machine passes, opens `onboarding` when it does not, remembers the step a flow was closed on, honours `DETTIVO_E2E_COMPLETE` and `DETTIVO_E2E_STEP`, and reopens through `dettivo app open onboarding`. Keys shows the compositor, the four bindings from `[hotkeys]` as key caps with the commands they run, the daemon-rendered snippet and its path, and a `Key check` panel that confirms a press from the `dictation.state` transition (compositor path) or `hotkeys.status.last_press_at` polled once a second (portal and evdev paths); Continue writes the snippet through `hotkeys.setup`; an unknown compositor shows the portal's outcome. Models lists the catalogue's shortlist (rows with `recommended_for`, plus ready or selected models) with size, reason and readiness, names the tier's default, follows the `model.download` events for the progress hairline and byte line, writes `[speech]` through `speech.selection.set` and starts `speech.models.download` on a pick, writes `[llm] provider` for the Enhanced tick (Ollama detection through `llm.providers.list`; the local model's download is a documented hook that fires once the daemon declares `llm.models.download`), holds Continue until the selected model is ready, and names a failed download's reason. Try it arms `insert.allow_self_target` while on screen, focuses the field, reads the completion event for `inserted via`, `stop to insert` and the mode, says so with the reason when there is no microphone, and Done disarms, records the completion and opens Home. The window drops the sidebar and the hint line while first run is on.

Daemon (crates/dettivod, dettivo-hotkeys, dettivo-insert, dettivo-proto): `hotkeys.snippet` and `hotkeys.setup` render and write the compositor snippet through the one generator `dettivo setup` now shares (`snippet::write`, idempotent, and `snippet::check` moved into dettivo-hotkeys); `hotkeys.status.last_press_at`; `insert.allow_self_target` (connection-scoped, app-only outside QA mode, the app's window ids only, disarmed on disconnect; `insert.perform.allow_self_target` and the session's insertion honour it, crates/dettivod/src/self_target.rs); `model.download` events published on the stream four times a second (they were only logged); `speech.models.status` rows carry `kind`, `languages` and `recommended_for` (catalogue entries for `parakeet-v3`, `large-v3-turbo`, `small.en`). The shared state file no longer erases itself: the daemon's `State` keeps foreign tables (`#[serde(flatten)]`), the app's `AppState` carries the daemon's tables through its save, and `[first_run]` holds `completed_at` and `step`. Visual: the `first-run` surface renders each step with the baselines' sample facts (`DETTIVO_E2E_SEED=1` under `--render`) and compares three regions per screen with rectangles cut from the step's own artboard (a manifest state may name its `artboard` and `crop`; the runner cuts the render before the diff); approved renders for Catppuccin Latte and both built-in palettes at 1x and 2x. QA: `first_run_fresh` and `first_run_provisioned` drives, the rig's `focus_pid` speaks Hyprland 0.56's Lua dispatcher, and the release-text lint and the negative text scan accept home-relative paths (`~/.config/...`) while still refusing absolute ones. Docs: docs/app.md first-run section, docs/qa.md, docs/hotkeys.md, docs/models.md, docs/insertion.md, docs/api/linux-deltas.md (four additions with fixtures), docs/design/README.md, docs/qa/a11y-names.md, qt/qml/Dettivo/README.md, ADR 0024 with its index row; CI runs both drives on both drivers under Xvfb.

Baseline: green (`cd /home/gordon/work/dettivo-linux-wt/fn-21 && XDG_DATA_HOME=$HOME/.local/share make build test lint` exit 0 at 47107f6 before any edit). Verify at 05e268235c9a1713b7b03eb99a78c5f96e63e405: the same command exit 0 (49 CTest cases, every cargo test); `XDG_DATA_HOME=$HOME/.local/share cargo run -q -p dettivo-qa -- contract` -> `contract: 69 passed, 0 failed, 20 skipped, 19 pending (not implemented yet)`; `cargo run -q -p dettivo-qa -- visual` -> `visual: 152 entries, 152 passed, 0 failed, 0 awaiting first approval -> pass`; `lint-scenarios: OK (17 scenarios use the driver interface only)`; `lint-accessible-names: OK (54 files, 64 documented names)`; `lint-release-text: OK (61 files)`.

Coverage: R1 crates/dettivo-qa/src/scenarios/first_run_fresh.rs and first_run_provisioned.rs (`just qa-drive first_run_fresh|first_run_provisioned atspi|cua`): on this desktop through the private-bus wrapper, `first_run_provisioned` pass on atspi (2.3 s) and cua (1.6 s), `first_run_fresh` pass on atspi (17.7 s; `first-run.json`: press line `F9 held · listening · Arctis Nova Elite Pro`, the snippet written once and byte-equal to `dettivo setup hyprland --stdout`, the test model downloaded 77 704 715 bytes to `ready` with `[speech] model = "test"` written and `tiny.en` written back, the field carried the fixture words, `stop to insert: 0.4 s`, `allowance_after_done: target_is_self`, `first_run.completed_at` in the state file); `first_run_fresh` on cua fails on this multi-output desktop at the first click (cua-driver's button coordinates land off the window, x = 4810 for a window at 1511; the same click path is proven on cua in CI's Xvfb by `insertion_matrix`), so the cua result for the fresh drive comes from CI; `DETTIVO_E2E_STEP` opening each step by name is the visual manifest's nine states plus CTest `dettivo-app-smoke-onboarding`; the trigger's four cases (provisioned, portal-bound, keys missing, model missing, VAD never counts) are qt/host/app/first_run_model_test.cpp firstRunIsNotRequiredOnAProvisionedMachine. R2 crates/dettivod/tests/hotkeys.rs snippet_and_setup_match_the_cli_and_write_idempotently (the daemon's text equals the CLI golden, `setup` reports then writes once with the mtime unchanged on a second write, sees the include line, never edits the main configuration, an unknown compositor is INVALID_PARAMS naming the supported set) and portal_presses_drive_sessions... (`last_press_at` after a portal press), crates/dettivo-hotkeys/src/snippet.rs write_is_idempotent_and_check_reports_the_include_state, the fresh drive (golden on screen through the tree and through `hotkeys.snippet`, the written file byte-equal, the live confirmation), first_run_model_test.cpp firstRunDecidesKeysAndConfirmsAPress (bindings from `[hotkeys]`, Continue writes, the recording transition confirms, the portal path for GNOME), tst_app_first_run.qml test_keys_shows_bindings_snippet_and_the_press. R3 first_run_model_test.cpp firstRunModelsPickWriteAndFollowTheDownload (the shortlist, the tier default, a pick writes `[speech]` and downloads, running/failed/done events with the reason, the Enhanced tick writes `[llm] provider` and the local download hook), the fresh drive (rows from the catalogue, the download to `ready`, `[speech] model` written twice), crates/dettivod/src/main.rs publish_downloads with the existing speech_models tests, tst_app_first_run.qml test_models_lists_rows_and_holds_continue_until_ready. R4 crates/dettivod/tests/insert.rs the_self_target_allowance_is_held_by_the_step_and_refused_outside_it (refused before arming, inserted while armed with the flag, refused without the flag, disarmed, ended with the connection, `notTheApp` outside QA mode), the fresh drive (the take through the real chain into the field, `inserted via` and `stop to insert` on screen, Done writes `completed_at`, the allowance refused after Done), first_run_model_test.cpp firstRunTryItArmsTheAllowanceAndReadsTheResult (arm, the result from the completion event, `not inserted` with the reason, Done disarms and completes, no microphone), crates/dettivo-core/src/state.rs the_apps_tables_survive_a_daemon_save and app_host_test.cpp stateFileRoundTrips. R5 qa/visual/manifest.toml `first-run` (9 states x 4 themes x 2 scales = 72 entries pass; Black Gold scores 0.616 to 0.975 against the artboard crops; crops by scripts/design/crop-baseline.py; approvals in docs/design/baselines.md), crates/dettivo-qa/src/visual tests, the negative text scan over every first-run tree in the fresh drive, `lint-accessible-names: OK (54 files, 64 documented names)` with the names in docs/qa/a11y-names.md. R6 docs/app.md (routes row, the first-run section, the QA variables, the drives), docs/api/linux-deltas.md (four rows with fixtures under crates/dettivo-proto/fixtures/hotkeys/ and insert/), `dettivo app open onboarding` in docs/app.md, docs/adr/0024-first-run-three-screens-config-writes.md with its README row; scripts/check-docs.sh passes.

Notes for the conductor: the drives on this desktop run through a private session and accessibility bus (the desktop's a11y bus is wedged, as for fn-17), and the fresh drive shares the live desktop with Gordon: the focus moved during the take on the passing run, so the drive's guarded hand-over (`insert.perform` with the app's window as guard and the allowance armed) landed the words and `focus_moved_during_take: true` is in the evidence; under Xvfb the take's own insertion lands. `/tmp` on this machine carries a user quota that other sessions' scratch nearly fills, so the fresh drive's models directory lives under `target/qa-first-run/` behind a link in the profile's data home and the fixture server serves the machine's tiny.en in place. `dettivo-engine-whisper` keeps answering `load_failed` for a good model after one unloadable model was loaded (seen when the drive's test model was fake bytes); the drive avoids it by serving a loadable model, and the engine's recovery is a follow-up for the engine spec. The sibling areas (history UI, LLM engine, meetings) were not touched; the Enhanced local row is the documented `llm.models.download` hook. `first_run_model.cpp` is 492 lines, under the limit.

stage: impl-review - skipped(config: REVIEW_MODE=none)
## Evidence
- Commits: d5175bc1776499535f5f1520b9e7489083ca7638, 40f05c0f2b585a192c4d67d8f1bd363b8b7736dd, f9648f55c9d252b4ff89d42a0e16fa4c6717e9d2, 89e0d387f83f9ff5d45cb5d6647763b0efe8e6d3, f9f436956d496bd7ba57b27f9033fed44aeed2cb, 5d10725028c68240d63aa727ce965946729d0dda, 41f1295bd93db60132cd29d1c863d4d3ab0897ad, 49e3dd6299390672e310138cbc6112c77d486150, 214bd3cb8e16544f7f024113d2bb4f145b31900b, 0c12ee110b70763ed611e5a0deca1a33acc33e9f, 66e1b41154d0889541187972734797ccc275b39a, 8dd8f10034effa8bf4f1d1ecde6f2c2a6a6b2e99, 4e62a89e1afa27009c48ec9392de7076611a59cc, b7b045edadc38355d6faf2087e3035e6cb9578aa, b0ef247fab6aa10cf6540e2e083170e6d096605d
- Tests: baseline: green (cd /home/gordon/work/dettivo-linux-wt/fn-21 && XDG_DATA_HOME=$HOME/.local/share make build test lint exit 0 at 47107f6 before any edit), cd /home/gordon/work/dettivo-linux-wt/fn-21 && XDG_DATA_HOME=$HOME/.local/share make build test lint (exit 0 at 05e2682: 49 CTest cases, every cargo test, fmt, clippy, edges, file length, deltas, qml lint and format, tokens, icons, accessible names, docs, rustdoc), cd /home/gordon/work/dettivo-linux-wt/fn-21 && XDG_DATA_HOME=$HOME/.local/share cargo run -q -p dettivo-qa -- contract (contract: 69 passed, 0 failed, 20 skipped, 19 pending), cd /home/gordon/work/dettivo-linux-wt/fn-21 && XDG_DATA_HOME=$HOME/.local/share cargo run -q -p dettivo-qa -- visual (visual: 152 entries, 152 passed, 0 failed, 0 awaiting first approval -> pass), cd /home/gordon/work/dettivo-linux-wt/fn-21 && cargo run -q -p dettivo-qa -- lint-scenarios (17 scenarios use the driver interface only), cd /home/gordon/work/dettivo-linux-wt/fn-21 && scripts/lint-accessible-names.sh (54 files, 64 documented names), cd /home/gordon/work/dettivo-linux-wt/fn-21 && scripts/lint-release-text.sh (61 files), cd /home/gordon/work/dettivo-linux-wt/fn-21 && cargo test -p dettivod --test hotkeys --test insert --test contract (snippet_and_setup_match_the_cli_and_write_idempotently, the_self_target_allowance_is_held_by_the_step_and_refused_outside_it, implemented_fixtures_pass_against_the_live_socket), cd /home/gordon/work/dettivo-linux-wt/fn-21 && build/qt/host/app/dettivo-first-run-test (6 passed) and build/qt/host/app/dettivo-app-host-test (13 passed), cd /home/gordon/work/dettivo-linux-wt/fn-21 && build/qt/qml/Dettivo/tests/dettivo-quick-test -input qt/qml/Dettivo/tests/qml/tst_app_first_run.qml (5 passed), fn21-a11y-drive.sh cargo run -q -p dettivo-qa -- drive first_run_provisioned --driver atspi (pass 2254 ms; private session and a11y bus), fn21-a11y-drive.sh cargo run -q -p dettivo-qa -- drive first_run_provisioned --driver cua (pass 1593 ms), fn21-a11y-drive.sh cargo run -q -p dettivo-qa -- drive first_run_fresh --driver atspi (pass 17673 ms; focus_moved_during_take true on this live desktop, the guarded hand-over landed the words), fn21-a11y-drive.sh cargo run -q -p dettivo-qa -- drive first_run_fresh --driver cua (fail on this multi-output desktop: cua-driver's click coordinates land off the window at the first Continue; CI runs it under Xvfb)
- PRs:
stage: plan-sync - skipped(config: planSync.enabled != true)
stage: completion-review - skipped(config: review.backend=none)
