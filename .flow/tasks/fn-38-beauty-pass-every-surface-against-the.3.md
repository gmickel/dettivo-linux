---
satisfies: [R8]
---
# fn-38-beauty-pass-every-surface-against-the.3 Repair release package layout and install verification

## Description
Repair the concrete packaging failures recorded in /tmp/dettivo-release-namcap.log and /tmp/dettivo-release-install-test.log. Move the native QML module installation and all path consumers to an appropriate private library directory, preserve static Omarchy data under share, satisfy both AUR package license identities, select the correct current-version application package even beside a debug package, and make namcap execution failures explicit. Preserve all approved QML appearance and runtime behavior.

**Touches:** qt/**, packaging/**, scripts/package.sh, scripts/packaging/**, scripts/test-build-entrypoints.sh, justfile, docs/**, .github/workflows/**, crates/** for installed QML path references, release CI classification, and the observed meetings export-dismissal race in the QA scenario.

## Acceptance
Satisfy R8, preserving all previously verified UI and keyboard behavior. Add focused regression coverage for debug-package selection and failed namcap invocation; prove the assembled package and all required installed checks. Await observed export-sheet dismissal before speaker rename, with bounded retries for transient accessibility trees; the seeded meetings scenario must pass on both drivers.

## Done summary
The release archive now places native QML modules and their consumers under /usr/lib/dettivo/qml, retains static Omarchy assets under share, and installs both AUR license identities. The strict 220-file manifest, exact current-version package selection and fail-closed namcap/install receipt checks prevent the observed broken packages from passing. The release gate recognizes positively confirmed disabled CI workflows while retaining failed or unexplained runs as blockers.

The meetings QA scenario waits for observed export-sheet dismissal before speaker rename and retries transient accessibility trees within its existing timeout. Fresh AT-SPI and CUA seeded drives pass, preserving the approved QML. Package creation passed real namcap with zero errors; prefix installation passed all 17 required checks plus install and icons. The installed Home render, Vulkan backend and CPU fallback were verified.

The combined candidate's final just build test lint result is recorded in the evidence JSON. The 31-minute isolated PipeWire soak completed three recordings with retained microphone/system tracks, live transcripts and completed finalization. Explicit postprocessing with the verified EN diarization model produced speaker labels and summaries for all three retained recordings. Its bounded receipt records the originally missing model and the third automatic summary still running at the final snapshot. No all-day, real-headset, multi-person speaker-accuracy or summary-accuracy acceptance is claimed.

Implementation review was skipped as requested. Release remains separate from task completion and requires Copilot-reviewed heads or Gordon's explicit exception, merged-code benchmark ancestry and a passing release receipt.
## Evidence
- Commits: e97c3ef7756990fe094feddbab9b000daf3ee22e, ed3207fea763df5d60d04f73f5982fbab57e7b0c, b8d4193b3fde75191f8bd1671f9d018a191cb602, 104e81c1c9e2d02d90ee7bf58e3478c289376ed1
- Tests: baseline: green via supplied handoff e97c3ef7; just build test lint (1012 Rust, 79 Qt), /tmp/dettivo-release-fixes-gate.log; source-equivalent metadata-only base 5105beff, red reproduction: mise exec just@1.58.0 -- bash scripts/test-build-entrypoints.sh; exit 1 because the real recipe selected app, debug and older version; /tmp/dettivo-packaging-selection-red.log, red reproduction: scripts/packaging/test-namcap.sh; exit 1 because the builder accepted namcap import failure; /tmp/dettivo-packaging-namcap-red.log, red reproduction: scripts/packaging/test-install-receipt.sh; exit 1 because a skipped required check passed; /tmp/dettivo-packaging-receipt-red.log, mise exec just@1.58.0 -- bash scripts/test-build-entrypoints.sh: passed exact current application selection and ambiguous/debug argument rejection, scripts/packaging/test-namcap.sh: passed execution failure, error and advisory warning cases, scripts/packaging/test-install-receipt.sh: passed skipped-check and failed-backend rejection, scripts/packaging/lint.sh: passed; optional shellcheck unavailable, cargo run -q -p dettivo-qa -- evidence-map --write: passed after adding four concrete R8 routes; /tmp/dettivo-packaging-repair-evidence-map.log, timeout 600 mise exec just@1.58.0 -- just build test lint: exit 0; 1012 Rust and 79 Qt tests, lint/docs passed; /tmp/dettivo-packaging-repair-gate-final.log, CMAKE_PREFIX_PATH=/home/gordon/.cache/dettivo-release-headers/root/usr DETTIVO_GIT_SHA=2f3f846d47cb30a3e5b78bb44235f25b4858ff32 timeout 600 mise exec just@1.58.0 -- just package-bin: exit 0, real namcap zero errors; /tmp/dettivo-packaging-repair-package-final.log, PATH=/home/gordon/.cache/dettivo-release-tools/root/usr/bin:$PATH DETTIVO_EXPECTED_GIT_SHA=2f3f846d47cb30a3e5b78bb44235f25b4858ff32 timeout 600 mise exec just@1.58.0 -- just install-test "--root build/install-root-release --models /home/gordon/.local/share/dettivo/models": exit 0; /tmp/dettivo-packaging-repair-install-final.log, Final receipt independently matched all 17 REQUIRED_CHECKS exactly once with pass/exit 0, plus install and icons; /tmp/dettivo-packaging-repair-install.json, Final layout inspection: both native QML libraries and qmldir files under usr/lib/dettivo/qml, both license files byte-identical to LICENSE, correct application RUNPATH, no shared libraries under usr/share, Final installed Home render visually inspected; /tmp/dettivo-packaging-repair-home.png, Artifact SHA256 receipts: /tmp/dettivo-packaging-repair-artifacts.sha256, Final combined just build test lint at 0e79924f: exit 0; /tmp/dettivo-rename-diagnosis/gate.log, Fresh release meetings_seeded drives after observed export dismissal fix: AT-SPI pass 2535 ms, CUA pass 7475 ms; /tmp/dettivo-rename-diagnosis, 31-minute isolated PipeWire fixture capture: three completed recordings and full retained tracks; docs/reports/qa/2026-09-14-meeting-soak.json, Explicit retained-audio diarization and analysis: all three ready with nonempty summaries after installing verified default EN model, Integrated source scripts/check-docs.sh: exit 0 after bounded soak report
- PRs: https://github.com/gmickel/dettivo-linux/pull/42