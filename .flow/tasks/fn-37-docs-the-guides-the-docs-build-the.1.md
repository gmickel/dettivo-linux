---
satisfies: [R1, R2, R3, R4, R5, R6]
---
# fn-37-docs-the-guides-the-docs-build-the.1 Docs: the guides, the docs build, the evidence map, the release gate and the release

## Description
TBD

## Acceptance
Every R-ID in the parent spec's ## Acceptance Criteria is satisfied; judge this task against the spec's criteria directly.

## Done summary
# fn-37: the guides, the docs build, the evidence map, the release gate and the reuse review

**R1.** A reader has one path through the product: `docs/guides/user.md` (install, first run, dictate, the modes, history, meetings, settings, when something fails), `docs/guides/agents.md` (the command tree, `--json` and the exit codes, MCP per host, REST, the capability flags, the delta register), `docs/guides/omarchy.md` (the one command, the keys, the bar and the panel, the pill, themes, plugin updates) and `docs/guides/qa.md` (the rig, the packs, visual, bench, the evidence map, the gate, how to write a scenario), each opening with what the reader gets and linking the topic pages rather than copying them. The agent guide's tree is generated: `dettivo docs cli-tree` (`crates/dettivo-cli/src/docs.rs`) walks clap's command graph and marks with `*` every path the new `CLI additions` table of `docs/api/linux-deltas.md` lists (a row marks the verb and everything beneath it; `--json` prints the tree as data), and the guide holds the output between `cli-tree` markers. Verified by the unit test `the_register_rows_mark_the_verb_and_everything_beneath_it`, by `scripts/check-docs.sh` comparing the block with the binary's output, and by `just docs` inside the gate.

**R2.** `scripts/check-docs.sh` keeps its ADR, link and contract-pin checks and adds four, each named by file and line: every `docs/**/*.md` is reachable from `README.md` by a breadth-first walk over links; a page's first paragraph under its H1 does not begin with Note, Warning, Caveat, Before you or This document (the pinned macOS copies exempt); every key of the `config.keys` fixture is named in `docs/config.md`; the guide's tree equals `dettivo docs cli-tree`, the first differing line named. `scripts/test-check-docs.sh` proves the build on a clone of the tracked tree with a baseline commit and five plants (an orphan page, a caveat-first `docs/install.md`, the `daemon.shutdown_timeout_ms` row removed, a renamed verb through a fake `dettivo`, an unindexed ADR) and runs as the second step of `just docs`; the CI `gate` job runs `just lint`, which runs both. Verified by running the plant test (`test-check-docs: ok (the clean tree passes; five plants fail by file)`) and by the gate.

**R3.** `qa/evidence-map.toml` maps every R-ID of every spec under `.flow/specs/` (40 specs, 247 R-IDs, 817 routes: 427 unit, 66 contract, 62 pack, 53 script, 36 drive, 33 visual, 118 docs, 6 bench, 4 pipeline, 12 human) and `dettivo-qa evidence-map` (`crates/dettivo-qa/src/evidence_map/`) reads each spec's acceptance criteria in both spellings the specs use, resolves every `ref` by kind (a `fn <name>(` under its crate or a Qt test under `qt/`, a fixture path, a pipeline name, a scenario id, a manifest surface, a pack step, a bench step, a page, an executable script, a receipt), writes `docs/reports/evidence-map.json` and `.md`, and exits 1 naming an unmapped R-ID, an R-ID the spec does not have or an unresolved reference with its kind. Coverage is 1.000 with every reference resolving. Verified by the fixture tests `full_coverage_passes_and_an_unmapped_rid_fails_by_name` and `an_unresolvable_ref_names_the_kind_and_the_reference`, by `the_checked_in_map_covers_every_rid_of_every_spec`, which runs the real map against the repository on every `cargo test`, and by the `evidence_map` step of the gate run.

**R4.** `dettivo-qa pack release` (`crates/dettivo-qa/src/pack/release.rs`, `release_steps.rs`, `release_checks.rs`, `release_report.rs`) runs `version_match`, `ci_green`, `contract_strict`, `mcp_harness`, `pack_dictation`, `pack_gui`, `pack_meetings`, `visual`, `visual_canary`, `bench_report`, `install_test`, `evidence_map`, `docs_build`, `notice_lint` and `real_headset` as whole commands, embeds each step's own report under `steps[].report`, and classifies every blocker as external (the CPU-only VM, a tool that is not installed, a row that fails on `cua-driver` while its `atspi` twin passes, GitHub Actions disabled, the live desktop an Xvfb session cannot reach, a person at the microphone) or unexplained; `--skip`, `--record`, `--cpu`, `--engines` and `--driver` are the switches, `just qa-release` and `make qa-release` the recipes. The executed run for the 0.1.0 candidate on this desktop is `docs/reports/release-gate/0.1.0.json` with its `.md`: run under `scripts/qa/xvfb-session.sh` with the release tree's Vulkan engines and the dettivo-bin package's prefix install test, verdict FAILED with 10 blockers, all 10 external and none unexplained (the CPU-only VM rows of the benchmark and the `--session` install test, the three meetings screens that fail on cua-driver and pass on atspi, the compositor and Omarchy steps the Xvfb session cannot reach, the runs API answering 404 because the workflows are disabled, the missing real-microphone receipt), and two failed steps on defects outside this spec: `contract_strict` because the daemon declares `automation.dictation_macros = true` and answers the six `automation.macros.*` methods as pending, and `pack_gui` because `history_seeded` and `history_states` expect 13 seeded rows and the list shows 15 since the seed gained four meetings. Verified by the unit test `the_release_pack_names_every_step_and_the_report_names_the_external_blockers` and by four executed runs on this desktop, the last at commit `0a032ffc`.

**R5.** `docs/reports/voxtype-reuse-review.md` walks every area of the plan's reuse policy (the daemon and CLI shape, the compositor snippets, the insertion tool order, MPRIS, the model download, meeting mode, the OSD, the model artifacts) with the Dettivo files and the verdict; every area is `clean-room` or `behavioural` and no span is a direct reuse, so `NOTICE.md`'s new reuse table (between `reuse` markers) is empty by design. `lint-notice` (`tools/xtask/src/notice_markers.rs`, `scripts/lint-notice.sh`) gains the rule that every `Reused from Voxtype (MIT), see NOTICE.md` marker has a row and every row has a marker, failing by path. Verified by the unit test `a_marker_without_a_row_and_a_row_without_a_marker_fail_by_path` (a planted marker, a planted row) and by `just lint-notice` inside the gate.

**R6.** `docs/RELEASING.md` is the gate step by step with the report it quotes; `docs/guides/qa.md` documents the evidence map's schema and kinds and the gate's report shape; `README.md` links the four guides under one heading (and `CONTRIBUTING.md`, the design record and the QA directory it did not link before); `CONTRIBUTING.md` states the same-commit rule for a behaviour change and names the four docs-build checks; `docs/qa.md` gained the reports index that makes every report reachable; ADR 0041 records the guides layout, the docs build checks, the evidence map, the gate script and the reuse rule, indexed in number order. Verified by `scripts/check-docs.sh`, which fails on an unindexed record, and by the gate.

### Verification

`flock /tmp/dtv-gate.lock make build test lint` green at the final commit (GATE_EXIT=0, run from `build/scripts/gate.sh` with `TMPDIR` under the worktree), `cargo run -q -p dettivo-qa -- contract` green (55 REST rows, every fixture), `cargo test -p dettivo-qa` (108 library tests including the real-map test), `cargo test -p xtask` (18), `scripts/test-check-docs.sh` ok, `dettivo-qa evidence-map` at coverage 1.000, and the release gate executed four times under `scripts/qa/xvfb-session.sh` (the AT-SPI registry started inside the session) with the run of record filed. The Vulkan engines were built from a scratch SDK under `build/vulkan-sdk` (the `vulkan-headers` package from the pacman cache and the SPIRV-Headers repository installed with CMake), because neither header package is installed on this desktop; the release tree, the `dettivo-bin` package and the prefix install test (18 checks, one skip for the missing `fish`) came from it. During the work the shared test model `~/.local/share/dettivo/models/whisper/tiny.en` vanished at 06:50 between two gate runs (something outside this worktree removed it) and was restored with `scripts/models/fetch-test-model.sh`; a run in between recorded 21 model-missing blockers and was superseded.

### Left out

No fix for the two failed gate steps, which belong to their owners: the pending `automation.macros.*` methods behind the declared `dictation_macros` flag (no macros spec exists), and the history list's seeded row count (fn-22's scenario expects 13, the fn-33/fn-34 seed shows 15). No fix for the three meetings screens on cua-driver (fn-34 and the driver). No real-microphone receipt (a person's step). No CPU-only VM rows. No rendered site. The `ci_green` step reads GitHub through `gh`; with the workflows disabled it records the 404 as the external blocker and the local gate stands in.
## Evidence
- Commits: f3f5eeb923473e161f6c8142445982552b531733, e5285f364f29ecac449c8f607f45c0a641453ee2, b8d7698712c7809cb7768599d53e007331556da0, 15ee6935ece71ceb963d33e8f825fcd0de6eea69, 0a032ffcd1cff33bfffc6d0b71b5881afa207d8c, 4ef48541a94e86881e64733027f56274f6af7e2f
- Tests: flock /tmp/dtv-gate.lock make build test lint, cargo run -q -p dettivo-qa -- contract, cargo test -p dettivo-qa, cargo test -p xtask, cargo test -p dettivo-cli, scripts/check-docs.sh, scripts/test-check-docs.sh, cargo run -q -p dettivo-qa -- evidence-map --write, cargo run -q -p xtask -- lint-notice, scripts/qa/xvfb-session.sh target/release/dettivo-qa pack release --engines target/release --driver atspi --out qa-evidence --record, scripts/packaging/install-test.sh --root build/install-root --report build/install-test.json build/aur/dettivo-bin-0.1.0-1-x86_64.pkg.tar.zst
- PRs:
stage: plan-sync - skipped(config: planSync.enabled != true)
stage: completion-review - skipped(config: review.backend=none)
