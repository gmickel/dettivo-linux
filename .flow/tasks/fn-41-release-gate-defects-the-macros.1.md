---
satisfies: [R1, R2, R3, R4]
---
# fn-41-release-gate-defects-the-macros.1 Release gate defects: the macros capability flag, the seeded row count and the QA profile's models directory

## Description
TBD

## Acceptance
Every R-ID in the parent spec's ## Acceptance Criteria is satisfied; judge this task against the spec's criteria directly.

## Done summary
R1: the release gate now passes on what the daemon does. `system.capabilities.automation.dictation_macros` and `dictation_macro_audit` are `false` on Linux (`crates/dettivod/src/platform.rs`), the snapshot fixture `crates/dettivo-proto/fixtures/system/capabilities.json` and the delta register (six `automation.macros.*` rows now `deferred` behind `automation.dictation_macros = false`) agree, and the contract replay learned the difference between an admitted gap and a broken promise: `replay_flags.rs` still sends a fixture whose required `true` flag the daemon declares `false`, a `NOT_IMPLEMENTED` answer there is a pending row carrying `admitted_by`, and `--strict` accepts those while failing a pending row behind a `true` flag or no flag, naming the method and the flag (`Report::broken_promises`). The proto fixture test accepts an admitted `false` for the same reason. Verified by `cargo run -q -p dettivo-qa -- contract` (85 passed, 0 failed, 44 skipped, 6 pending, 6 behind a false flag) and `-- contract --strict` (exit 0), by the unit tests `a_false_flag_admits_a_pending_row_and_a_true_flag_promises_it` and `capability_flags_resolve_in_the_snapshot`, and by the gate's `contract_strict` step, which passes with the six macros methods listed as admitted gaps.

R2: `history_seeded` and `history_states` read their expectation from the seed they run against. `scenarios/history_seed.rs` asks the seeded daemon for `transcripts.list` with the kinds the History list requests (`dictation`, `meeting`) and for `transcripts.search` with the field's request, and both drives wait for that many rows and that many hits; a mismatch names the count shown and the count the seed answered. The seed is sixteen rows today and the view keeps only the delegates near the screen, so the wait knows the fold: fewer rows are accepted when they are the seed's leading rows in order and the list is full past its bottom edge (or unbounded, on a driver that bounds only what it can act on), and a re-run is seen as the row that was on top pushed down. The query is typed until the field carries it, because the first keystroke after a click was lost once under load. Verified by the `history_seed` unit test, by both drives passing on `atspi` under `scripts/qa/xvfb-session.sh`, by the gui pack's history surface passing on `atspi` and `cua`, and by `docs/qa.md`'s two rows saying where the count comes from.

R3: no QA profile links a real user directory. `crates/dettivo-qa/src/profile_models.rs` builds one private tree per profile under `target/qa-models/` (the models' filesystem, so every link is a hard link; the `/tmp` root is a quota-bound tmpfs where a copy of `large-v3-turbo` would not do), points the profile's `data/dettivo/models` at it, links `TEST_MODELS` (`whisper/tiny.en`, `fixtures`) when the machine has them, and links anything else by name through `Profile::link_model`, refused by name with the fetch command when the machine lacks it; `profile_models::guard` refuses a tree that is the real directory or under it, by path and through symlinks, before anything is created. `Scenario::models` names what a drive needs beyond the test set (`meeting_token_coverage` names `diarize/diarization`) and the runner links it before the preconditions or skips the scenario by name; the bench, the meetings throughput step, the polish evaluation and both contract daemons name theirs at the call, and the two duplicated `link_test_model` helpers are gone. Verified by three unit tests (the tree carries only the named models as hard links and a delete under it leaves the real file; the guard refuses the real directory, a path under it and a symlink to it; the profile builds, links by name and removes its tree on drop) and by `pack gui` and `pack dictation` under Xvfb, each bracketed by a listing of `~/.local/share/dettivo/models` (path, size, mtime, inode, link count, checksum) that stayed byte-identical to the baseline, as did the listing after both release-gate runs and at the end.

R4: `dettivo-qa pack release --engines target/release --record` re-ran under `scripts/qa/xvfb-session.sh` on the Vulkan engines (a scratch SDK under `build/sdk` from the pacman cache's `vulkan-headers` plus an installed SPIRV-Headers checkout, `scripts/package.sh` for the release tree, the AUR package and a prefix install test against a hard-linked private models copy). The run of record `docs/reports/release-gate/0.1.0.json` and `.md` is `passed` at commit `2582ff95409a`, 1 149 981 ms, ten blockers all external and none unexplained: `ci_green` (workflows disabled), `hotkeys_hyprland`, `omarchy_bar` and `omarchy_setup_idempotent` (the live desktop), `meetings_seeded`, `meetings_live_gui` and `meetings_import_gui` on `cua-driver` (passed on `atspi`), `bench_report` and `install_test` (the CPU-only VM), and `real_headset` (the receipt). ADR 0041 carries the outcome and ADR 0043 records the three decisions; the meetings pack re-recorded its figures into `docs/reports/benchmarks/2026-09-06-thor-gpu.json` from the same run.

Verification: `flock /tmp/dtv-gate.lock make build test lint` is green at `71abf9ee` (59 Qt tests, every Rust suite, fmt, clippy, the edge, file-length, delta, notice, packaging, QML, Omarchy and settings-key lints, `docs: ok`, `test-check-docs: ok`); the evidence map gained fn-41's routes and `dettivo-qa evidence-map --write` regenerated the checked-in reports; every drive and pack ran through `scripts/qa/xvfb-session.sh`, never on the live desktop.

Left out on purpose: no macros implementation (the flag tells the truth and a later spec flips it back), no change to the seed, no cua-driver fixes for the meetings screens (external blockers owned by fn-34 and the driver), no CPU-only VM run and no real-microphone receipt. Two adjacent findings are reported rather than fixed: `scripts/packaging/install-test-checks.sh` still symlinks the models directory it is given into its temporary data home (the gate run used a hard-linked private copy through `--models`), and the first release-gate run exposed a race in `meetings_import_gui` where a fast engine opened the detail before the list was read; that wait now accepts the detail, a one-function change in an fn-34 scenario because the gate's atspi row depended on it.
## Evidence
- Commits: b8550c929cbb863711d6ce16305aea41e2e1267c, c9f62b3f9ce88318945ca77e74e156384d77c074, 9524f98808e10fd618372609718e5807856dcf31, 29b11f8f6496f293f98628267013aea3eb0b98b9, c932bbabe7c5daa25312db31c4cc0ec146200241, 2582ff95409a9eca69389092e5537450440bc61b, 8d8b4a5f004e0a758bf249d2d8939d7ec376c39b, e9c0aa0ad0a7025ce6598f3235cb85a14d73f333, 71abf9eeb370af50c4c7d6e32922636f098200f9, b2e262feff0b4b320c3bfa5500f5c3b12c2052f5, d9430c1b0c428043f4cde64eb972216cf8840107
- Tests: flock /tmp/dtv-gate.lock make build test lint, cargo run -q -p dettivo-qa -- contract, cargo run -q -p dettivo-qa -- contract --strict, cargo test -q -p dettivo-qa --lib -- replay profile history_seed, cargo test -q -p dettivo-proto --test fixtures, scripts/qa/xvfb-session.sh target/debug/dettivo-qa drive history_seeded --driver atspi, scripts/qa/xvfb-session.sh target/debug/dettivo-qa drive history_states --driver atspi, scripts/qa/xvfb-session.sh target/debug/dettivo-qa pack gui --continue --driver atspi, scripts/qa/xvfb-session.sh target/debug/dettivo-qa pack dictation --continue --driver atspi, scripts/qa/xvfb-session.sh target/release/dettivo-qa pack release --engines target/release --record --continue, cargo run -q -p dettivo-qa -- evidence-map --write, scripts/check-docs.sh
- PRs:
stage: plan-sync - skipped(config: planSync.enabled != true)
stage: completion-review - skipped(config: review.backend=none)
