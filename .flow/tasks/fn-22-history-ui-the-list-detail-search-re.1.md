---
satisfies: [R1, R2, R3, R4, R5, R6]
---
# fn-22-history-ui-the-list-detail-search-re.1 Implement fn-22: the history list, detail, search, re-run dialog, export sheet and delete, with drives and baselines

## Description
TBD

## Acceptance
Every R-ID in the parent spec's ## Acceptance Criteria is satisfied; judge this task against the spec's criteria directly.

## Done summary
Built History as a place: the list column beside the dictation detail, to history.png. The daemon side first: transcripts.get carries a Linux facts block (created_at, app, source, status, provider, model, language, duration, title, rerun_of, audio retained or why not, the insertion as reported with its backend, and the stop-to-insert split stored with the item by migration 0004-timings, absent on older rows), transcripts.search paints its hits (character ranges inside the snippet under the same word-prefix rule the FTS expression applies, accent-folded) and lists each hit as a HistoryItem, and HistoryItem rows carry app_id, mode and source so a row has its meta line and the kind filter has a field; every addition is registered in docs/api/linux-deltas.md with the fixtures updated (list, search, stats) and omitted on the wire where absent so the macOS shapes stand. The seed now carries the mock insertion backend and a timings split on every row, and with keep_audio on and audio_retention_days = 0 the daemon writes one second of silence as the take of row 11 (the design-call summary) and attaches it to the row without touching updated_at, so a drive can re-run an item while the contract sample keeps no audio for the audio_not_retained fixture; the export goldens were regenerated and the command-line export test levels the one audio path. DETTIVO_E2E_EXPORT_DIR joins the QA variables on both sides, and dettivo app open history.detail --id <uuid> reaches a detail through the instance socket's arg.

The app host gained HistoryModel's search (the query swaps the rows for hits with painted ranges and back), job tracking (a re-run's job id maps to its row; the progress hairline and the stage follow job.progress; a job the command line started settles on the next page read) and a local remove; HistoryFilterModel for All, Dictation, Meeting and Import; HistoryDetailModel over the facts block (the when line, Enhanced and Raw texts, the mode and outcome labels, the raw meta with the transcribe time, the two-column facts, canRerun with the reason); HistoryActions (transcripts.rerun with a chosen engine and model, transcripts.delete, insert.perform with the item as source, and an export through transfer.begin, transcripts.export and transfer.pull into a file the org.freedesktop.portal.FileChooser SaveFile dialog names, or the QA export directory under the daemon's file name); and HistoryPlayer (QMediaPlayer with QAudioOutput over the take, the strip's 48 bars read from the WAV samples in the host, seek by fraction, the clock label). Qt Multimedia joins the build and the toolchain packages. The sample carries the artboard's three days of rows, the detail and the take, a render with DETTIVO_E2E_SEED=1 and no daemon uses it like --sample and opens the newest row, so the visual manifest renders History from dettivo-app with the same variables as every surface.

The QML lives under qt/qml/Dettivo/app/history/: HistoryRoute (the list beside the detail; / focuses search, Escape gives focus back before the router closes the detail, j and k move, Enter opens, r, e and d run the actions; the window's hint line names them on History), HistoryList (the compact heading with the count, SearchField with the 2 px focus border, the hit count and the clear glyph, FilterChips, the day headings and HistoryRow with the time, one line of text with Html.highlighted spans in the 12 % accent fill, the meta line, the Meeting chip, the selection rail and the progress hairline; the designed empty and no-match states), HistoryDetail (the heading with Insert again, Re-run and Export, the when line, the action outcome line, DetailBlock for Enhanced and for Raw in the light weight, AudioStrip with the played portion in the accent, FactsGrid, the re-run progress line and delete as text in the urgent colour), RerunDialog (engine and model from speech.providers.list with the daemon's selection picked first, the mode pinned to Raw naming ADR 0023, the daemon's refusal named), ExportSheet (format, scope, the destination line, the outcome) and DeleteConfirm (the state sentence with the urgent square, the reason, Delete for good as urgent text). Every element has an accessible name; the new names are listed and documented, and the clear glyph, the rows and the chips carry a press action for the accessibility drivers.

Visual: the manifest's history surface renders the list, the detail and the facts and cuts each render to its crop rectangle before comparing (a new step in dettivo-qa visual and approve, scaled with the entry), the three Black Gold crops are cut from history.png by crop-baseline.py (scores 0.67, 0.59, 0.78), and the other four themes are approved renders recorded in docs/design/baselines.md. Drive: history_seeded on either driver walks the days and the twelve rows, the search for api with its 2 hits and the painted rows, the detail of the row with the take (Enhanced, Raw, Audio, Play, insertion: mock, stop to insert: 0.8 s, app: TextEditor, engine: whisper · large-v3-turbo), an export of everything as JSON compared with the store's golden, a re-run through the dialog with tiny.en until the linked item settles, and a delete after the confirmation that leaves twelve rows and the store without the item. Docs: docs/app.md's History section, keys and variables; docs/history.md, docs/qa.md, docs/qa/a11y-names.md, docs/design/README.md; ADR 0025.

Baseline: not isolated (the pre-edit `make build` was started before the first edit but edits landed while it ran; main at 341744c is green in CI). Verify: XDG_DATA_HOME=$HOME/.local/share make build test lint exit 0 (the Makefile mirror; just is not installed here; 49 CTest cases including the new dettivo-app-history-test and the two history Quick tests), cargo run -q -p dettivo-qa -- contract: 65 passed, 0 failed, 20 skipped, 19 pending; cargo run -q -p dettivo-qa -- visual: 110 entries, 110 passed; drive history_seeded --driver atspi: pass (three passing runs, 9 to 11 s; two failed runs in between when sibling drives on the shared desktop stole focus mid-scenario); --driver cua: fails on this scaled Hyprland desktop at the clear glyph (the pixel rung lands beside a 16 px control while the atspi driver's XTEST click lands on it); CI runs both under Xvfb at 1x, where no scale applies. Xvfb is not installed here, so the Xvfb path is unverified locally.

Coverage: R1 crates/dettivo-qa/src/scenarios/history_seeded.rs (day labels, twelve rows, 2 hits and the painted row, the detail's texts, strip and facts with backend and stop-to-insert, the negative text scan on every capture), qt/host/app/app_history_test.cpp listSearchesPaintsFollowsJobsAndDrops and detailReadsTheFacts, qt/qml/Dettivo/tests/qml/tst_app_history.qml and tst_app_history_detail.qml, crates/dettivod/tests/history.rs get_carries_the_facts_and_a_search_hit_paints_its_matches, crates/dettivo-storage/src/search.rs match_ranges_follow_the_prefix_rule_with_accents_folded; R2 the drive's re-run to a settled linked item (tiny.en over the seed's take) and its export compared with seed.json, app_history_test actionsRerunDeleteAndExportThroughTheLink (the refused re-run names the reason, the export lands in the QA directory), tst_app_history_detail Re-run disabled without a take, the daemon-backed re-run tests already in crates/dettivod/tests/history_model.rs; R3 the drive's delete and app_history_test's remove, tst_app_history_detail's confirmation; R4 qa/visual/manifest.toml history with crops, docs/design/studio/baselines/history/, crates/dettivo-qa/src/visual cut step, a missing crop still fails by name through the matrix; R5 the shortcuts in HistoryRoute and AppWindow, tst_app_history route test (focusSearch, dismiss), scripts/lint-accessible-names.sh over the new files; R6 docs/app.md, docs/api/linux-deltas.md rows, docs/adr/0025-history-detail-facts-playback-and-export-file.md, docs/qa.md.

Notes for the conductor: the cua driver on this HiDPI desktop misclicks small controls (see ADR 0025's last consequence); if CI's Xvfb run of history_seeded on cua fails at Clear search, the fallback is to clear the query through the field's own text (select all plus a retyped empty query) rather than the glyph. Sibling drives on the shared desktop make live drive runs flaky; the receipts of the passing runs are under qa-evidence/run-1788556886-1234799 and the later passing run. QMediaPlayer's error (no audio output) shows on the strip's clock line; the take still draws. The list's search filters to the hits rather than highlighting within the timeline (the design's no-match state names the query and keeps the filter, which only reads as a filtered list). Rows show the FTS snippet while a query is set (the matched words may sit outside the title's 48 characters). Meeting rows carry the chip and their length only; speaker counts arrive with the meetings GUI. Files over 400 lines: qt/host/app/history_model.cpp (451), qt/host/app/app_host_test.cpp (461, pre-existing).

stage: impl-review - skipped(config: REVIEW_MODE=none)
## Evidence
- Commits: 8f8ed65904fdfd33babc038b0faa60eec65281e3, d3c1f266916db1df9b658e69dfe6e0b75caffdb9, ab53903a037341a432dbec5c79b94f68919c813f, 5cd8c3a10e5957eee6eda52c564eb9a61bad43e6, 41cc2e111b0b788ea0c97b92990aca04299f31fc, 7fe8f89718db45e809b6fc2328912d360287042a, ed91e4a53b45e20408d45876eb9a3ccc3a52d7de
- Tests: baseline: not isolated (pre-edit make build started before the first edit; main 341744c green in CI), XDG_DATA_HOME=$HOME/.local/share make build test lint (exit 0; 49 CTest cases), XDG_DATA_HOME=$HOME/.local/share cargo run -q -p dettivo-qa -- contract (65 passed, 0 failed, 20 skipped, 19 pending), XDG_DATA_HOME=$HOME/.local/share cargo run -q -p dettivo-qa -- visual (110 entries, 110 passed), cargo run -q -p dettivo-qa -- drive history_seeded --driver atspi (pass, 9 s; private accessibility bus), cargo run -q -p dettivo-qa -- drive history_seeded --driver cua (fails on the scaled Hyprland desktop at Clear search; CI runs it under Xvfb), cargo test -p dettivod --test history --test contract --test history_export --test history_model, cargo test -p dettivo-storage, cargo test -p dettivo-cli --test history --test app, cargo test -p dettivo-core --lib qa, cargo test -p dettivo-qa, ctest --test-dir build/qt -R dettivo-app-history-test|dettivo-app-host-test|dettivo-quick-test|dettivo-app-visual
- PRs:
stage: plan-sync - skipped(config: planSync.enabled != true)
stage: completion-review - skipped(config: review.backend=none)
