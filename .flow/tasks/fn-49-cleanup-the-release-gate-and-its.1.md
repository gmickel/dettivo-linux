---
satisfies: [R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15, R16, R17]
---
# fn-49-cleanup-the-release-gate-and-its.1 Cleanup: the release gate and its evidence, every finding fixed or rejected with evidence

## Description
TBD

## Acceptance
Every R-ID in the parent spec's ## Acceptance Criteria is satisfied; judge this task against the spec's criteria directly.

## Done summary
# fn-49 Cleanup: the release gate and its evidence

Every finding was verified against the code first (a failing test or a reproduced run), then fixed with a regression test. No finding was rejected: each claim held on inspection. Sixteen findings fixed, zero rejected. Branch rebased onto main a47c0c5; 14 commits over the minted task commit (13 findings/docs commits plus c4a7eaa, which splits `pack/binaries.rs`, `pack/release_blockers.rs` and `tests/contract/normalise.rs` out of the three files the fixes pushed over 500 lines).

### Findings

| ID | Finding | Verdict | Commit / evidence |
|---|---|---|---|
| R1 | daemon/F11 fixture normalization replaces evidence with the expected answer | fixed | 671c3bd: `crates/dettivod/tests/contract.rs` deserialises the search rows as the protocol types, requires the fixture's hit among the seeded answer and its snippet inside the answer's, and copies nothing from the fixture; `an_empty_search_result_never_passes_as_the_fixture` and `a_malformed_search_row_never_passes_as_the_fixture` are the negative checks. The `replay.rs` shape levelling the finding also names is qa-packs/F9 in fn-50 and was left to that worker. |
| R2 | engines/F15 the Parakeet build modifies a caller's source checkout | fixed | 26a3720: `crates/parakeet-cpp-sys/patch.rs` (shared by `build.rs` and `tests/patch.rs`); a supplied `PARAKEET_CPP_SOURCE_DIR` tree is only checked with `patch -R --dry-run` and the build stops naming the command when the patch is missing; the owned fetched tree is patched once. Tests: `a_supplied_tree_is_checked_and_never_written`, `an_owned_tree_is_patched_once_and_a_tree_without_a_patch_builds_as_it_is`. `docs/engines.md` updated. |
| R3 | qa-rig/F8 accessibility failures can become 100% naming coverage | fixed | 3b8797a: `a11y_tree::check` keeps controls with unknown geometry (only zero-size is hidden); the AT-SPI snapshot errors on the depth/size limit and on an unreadable child instead of returning a smaller tree. Test `a_control_whose_geometry_failed_to_read_still_has_to_carry_a_name` (failed before: the control vanished). Extents failures themselves stay `None` and are counted, so a transient extents failure never aborts a drive. |
| R4 | qa-packs/F1 publication does not enforce the documented release gate | fixed | 685ea05: `scripts/packaging/release-receipt.sh` runs in `prepare` when publishing: the checked-in `docs/reports/release-gate/<version>.json` passed, no failed step, no unexplained blocker, gated commit an ancestor of the released one with nothing but `docs/reports/` changed since; reads report and history only. `test-release-scripts.sh` (in `lint-packaging`) covers missing, failed, stale, corrupt, mismatched and matching receipts. |
| R5 | qa-packs/F2 the install gate accepts empty, stale or incomplete receipts | fixed | 90b5897: `gate::judge` is typed; verified first by `a_receipt_without_evidence_is_refused` failing with "empty steps passed" on the old code. Requires the 17 checks once each, `pass` with exit 0, version and package of this release; a missing report fails as unproven and the `--session` classification rule is deleted. |
| R6 | qa-packs/F3 the release wrapper converts unexplained failures into external exceptions | fixed | dd7db61: `judge_pack` fails a pack step on any failed row whichever driver; the "failed on cua-driver" rule is deleted (the 0.1.0 record's meetings step passed after exit 1 with three failed cua rows, confirmed in `0.1.0.json`). `ci_green` asks `gh api repos/{owner}/{repo}/actions/permissions`; external only when Actions is confirmed disabled. Tests `a_row_that_fails_on_one_driver_fails_the_step_whatever_the_other_did`, `missing_ci_evidence_is_external_only_when_the_api_confirms_actions_are_disabled`. |
| R7 | qa-packs/F4 reports identify the checkout rather than the binaries tested | fixed | 9019c2c: `DETTIVO_BUILD_PROFILE` (default `debug`) chooses `target/<profile>/`, the mtime choice is deleted; `BinaryInfo` carries `sha256` and the CLI's embedded `revision`; `pack release` pins `release` for itself and every spawned verb and refuses (exit 2) a debug or stale `dettivod`/`dettivo`. Tests `the_profile_chooses_the_binary_and_a_newer_file_in_the_other_profile_never_does`, `the_release_gate_refuses_a_debug_or_stale_binary_set`. Rig sets the variable for the GUI pack. Only the CLI embeds a commit; the Qt apps and dettivod are bound by hash and profile. |
| R8 | qa-packs/F5 the benchmark release check validates filenames, not measurements | fixed | 2668e8c: new `pack/release_bench.rs` judges the typed report (parse, tier, steps, no failed/valueless step, commit and meetings block in history, meeting and diarization factors); CPU tier accepts any host's clean report; `--record` writes the meetings block into the latest report in place; the 2026-09-06 copy folded into the 2026-09-05 report (the README had been showing the older block hidden by the copy); the `diarization` placeholder step deleted from the bench suite. Tests `a_report_passes_on_its_contents_and_never_on_its_name`, `a_cpu_only_host_clears_the_forced_desktop_row_and_a_corrupt_newest_file_never_stands_in`, `the_block_lands_in_the_latest_report_in_place_and_never_under_a_new_date`. |
| R9 | qa-packs/F7 evidence-map coverage is not evidence of requirement completion | fixed | eea51c1: `unit` refs require `#[test]`/`#[tokio::test]` (all 432 existing refs already were tests), `visual` refs resolve `surface/state` against the manifest, `human` refs resolve as receipts; report and docs name the map an inventory of routes; duplicate `BENCH_STEPS`/`PIPELINES` lists deleted in favour of `bench::STEPS`, `bench::meetings::ROWS`, `pipeline::PIPELINES`. Test `a_unit_ref_needs_a_test_attribute_and_a_visual_ref_needs_a_real_state`. Human receipts are still files; requiring a typed approval receipt per human route was left out (see below). |
| R10 | qa-packs/F16 missing accessibility captures are reported as clean coverage | fixed | 3b8797a: `scan_dir` reads `tree.json` as the `window` route and reports an empty tree as a finding; a GUI step (one with a surface) that passed without a captured tree fails as not inspected; `a11y_coverage` is `null` for a surface with no inspected tree (the markdown test that expected `1.000 (0 of 0 named)` for an uninspected surface was the claim itself). Tests `an_unreadable_or_empty_tree_is_a_finding_and_the_default_tree_is_scanned`, surfaces/markdown tests updated. Checked against the 0.1.0 record: every passed GUI step there captured at least one tree. |
| R11 | qa-packs/F17 the startup benchmark includes profile preparation | fixed | ffdbd78: `BenchDaemon::prepare` / `Prepared::spawn`; `startup` times spawn to ping through `spawn_times`, tested by `the_startup_clock_excludes_the_profile_preparation` (60 ms preparation, 5 ms spawn, samples under 50 ms). The config file write inside the shared launcher stays inside the clock. |
| R12 | qml/F15 the shared style checker examines only one delegate | fixed | 3e90834: verified first with the new `basic_content` plant against the unfixed checker: six CTest cases failed (no `contentItem from` finding); after `delegateFindings` checks both delegates independently, 12/12 pass. Plants `basic_background`/`basic_content` added to `PLANTS`, `kPlants`, CMake tests on sheet, pill and bar, docs. |
| R13 | ops-and-record/F1 a release rehearsal against a tag ignores `dry_run` | fixed | 685ea05: `scripts/packaging/release-decide.sh`; tag push publishes, dispatch publishes only with `dry_run=false` on the tag, a rehearsal never publishes whatever the ref; `test-release-scripts.sh` covers tag push, branch and tag rehearsals, unset `dry_run`, publishing dispatch on/off the tag, mismatched tags. |
| R14 | ops-and-record/F7 the release gate accepts empty or stale install evidence | fixed | 90b5897 (same fix as R5); package checksum binding was not added because the receipt does not carry one; the package name must name the version. |
| R15 | ops-and-record/F8 the benchmark gate accepts unreadable evidence | fixed | 2668e8c (same fix as R8); a squashed-branch measurement is now a rejection naming `just bench` on a commit of this history rather than an inferred explanation. |
| R16 | ops-and-record/F13 releases never update the documented plugin mirror | fixed | b64d9d6: the promise is removed; `docs/omarchy.md` and `docs/RELEASING.md` name the export as the releaser's hand step after the tag, no workflow publishes the mirror. A publication stage was not added (it needs a token for a separate repository and a rehearsal into a disposable checkout). |

### ADR

`docs/adr/0051-the-release-gate-passes-on-validated-evidence-bound-to-the-tested-binaries.md` (a53a358), indexed in `docs/adr/README.md`; amends 0030, 0034, 0039, 0040 and 0041, each carrying a pointer forward on its status line. `docs/RELEASING.md`, `docs/qa.md`, `docs/guides/qa.md`, `docs/omarchy.md`, `docs/engines.md` updated with the finding they document. `qa/evidence-map.toml` carries fn-49's routes (regression tests, release scripts, pages) in place of the placeholder.

### Verification

- `flock /tmp/dtv-gate.lock make build test lint` on the final commit c4a7eaa (rebased onto main a47c0c5): every step through `[rustdoc] rustdoc` passed, `gate exit 0 at 2026-09-06T14:32:12Z`; `~/.local/share/dettivo/models` listed before and after, byte-identical. The first run on a53a358 failed only `lint-file-length` (release.rs 511, report.rs 529, contract.rs 551), fixed by the split commit.
- `cargo test -p dettivo-qa --lib` (all pack, bench, a11y, scan, evidence-map tests), `cargo test -p dettivod --test contract` (7 passed), `cargo test -p parakeet-cpp-sys --test patch` (2 passed), `ctest --test-dir build/qt -R style-check` (12/12 after, 6 failed before), `scripts/packaging/test-release-scripts.sh`, `scripts/packaging/lint.sh`, `cargo clippy -p dettivo-qa --all-targets`.
- No drive, pack or visual run was needed: no fix touched a scenario's behaviour, and the Qt style checks render offscreen.

### Deliberately left out

- qa-packs/F9 (`replay.rs` shape levelling, cited inside daemon/F11) belongs to fn-50 and was not touched.
- A typed human-approval receipt per `human` evidence route: the map now names human routes as receipts, not proof; a receipt schema is a decision for the owner.
- A mirror publication stage: removed the promise instead (needs a separate-repo token and a byte-for-byte rehearsal).
- Binding dettivod and the Qt apps by an embedded commit: only the CLI embeds one; the others are bound by SHA-256 and profile.
- The 0.1.0 release-gate record was left as history; under the new rules it is not a passing run (three failed cua rows, squashed-branch benchmark commits, unconfirmed `ci_green`). The next gate run on a commit of main is the run of record.
- The Makefile mirror (ops-and-record/F15) and the other packaging findings are fn-51's.
## Evidence
- Commits: 3c8233997022af57ba53c7b1f162839a745ab6c4, 3da018762d511def38b286acaba097fdd497c5f8, 74337de7fe024126371893da7356471c7ac11ef8, bfd8e980740a9470188cd9b00db812ecff6198ef, 193280dd263f3f2796f4ea8072daaa313c1ad275, e5bfe76023b635c936c512d3ebd42c3a4b9a3bba, 36bef38bcf5d9d2e3465a302b06dbbb1605eb7dc, 63257b98c0819447dc3728247b1e342bae241067, 31fe1ed86376f7a6c228d7624ea1d94f58bc6215, a28ff2d069563aba21d9d27bf64bfa4f06e20c36, 76b90f80f6ce7b4b30872d94d2f22b12247b20c0, 56a83e735d144ace67ed4b7245cd420198411661, 41d3e88637d699b665e36dea76030545d9e59854, 79d0fc9edbb4c1b0c5ff9ce7f2251ef927655871
- Tests: flock /tmp/dtv-gate.lock make build test lint, cargo test -p dettivo-qa --lib, cargo test -p dettivod --test contract, cargo test -p parakeet-cpp-sys --test patch, ctest --test-dir build/qt -R style-check --output-on-failure, scripts/packaging/test-release-scripts.sh, scripts/packaging/lint.sh, cargo clippy -p dettivo-qa --all-targets
- PRs:
stage: plan-sync - skipped(config: planSync.enabled != true)
stage: completion-review - skipped(config: review.backend=none)
