# fn-62-fix-meeting-timer-stuck-at-0000-in.1 Implement ticking meeting status clock and final status-caption QA correction

## Description
Implement the parent spec's timer goal and its native-QA caption follow-up directly. Preserve original recording/data and use the existing Omarchy theme and plugin patterns. The conductor handles the final full gate and local install; no full release. Judge this task against the parent spec's acceptance and observed cause.
## Acceptance
The parent spec timer acceptance is satisfied, including silent recording, reconnect, stop behavior, and ownership across overlapping old-meeting post-processing/new-meeting recording. Old meeting events cannot clear or rewind the new recording clock; explicit no-meeting reset still clears it. The native-QA speaker status caption from fn-63 keeps its unassigned count and meaning visible. At the default/minimum supported size, the detail footer's shortcut hints do not overlap the separate delete-meeting control; title renaming remains discoverable in visible hints and full keyboard help.
## Done summary
The Omarchy meeting clock now ticks independently of speech, restores its recording anchor from snapshots, stops its recording indication during transcription, and ignores older meetings' late processing events. The speaker-stage detail retains its unassigned count, and meeting-detail shortcuts fit beside Delete at default and minimum window widths.

stage: impl-review - skipped(config: REVIEW_MODE=none)
stage: implement - ran (model: claude-fable-5-1 at high; delegated: 0)

baseline: none (no Quick commands in spec). The conductor owns the final full just build test lint gate, native ticking preview and local deployment.

Focused final verification: 48 Qt/Omarchy cases passed (19 plugin, 5 processing, 5 states, 6 detail, 7 meetings, 6 bar); file-length, Omarchy lint, QML lint, docs, packaging and diff whitespace checks all passed with observed exit code 0. Final logs: /home/gordon/.cache/dettivo-qa-tmp/fn62-final-*.log. Tests used the authorized bwrap /tmp binding and synthetic data only.

Regression evidence: timer and caption tests failed before the fix; follow-up glyph-state, footer overlap, shortcut-help and clock-ownership assertions also failed for their intended causes before correction. Red/green logs and implementation digest: /home/gordon/.cache/dettivo-qa-tmp/fn62-task1-digest.md and fn62-*.log.

No runtime configuration, installed plugin, daemon, original meeting, package, push or release was changed.

Conductor validation: complete just build test lint passed on the combined local hotfix (all Rust tests,81 Qt checks, lint and docs). The command used the installed Just1.58 binary and an isolated disk-backed /tmp mount because the host user quota was full; no tests or assertions were skipped. Native click/T rename, Enter save, Escape cancel and reopen persistence passed on the isolated meeting. Final default-size render confirms the status and footer fit. The local standalone UI bundle, daemon and plugin were installed and verified by hashes/process paths. Actual saved-meeting analysis reached ready with8 calls/6 parts under unchanged model limits. Original title/transcript/948 segments/notes/audio and config hashes remained unchanged. No full release; package remains0.1.0-1.58. Hosted CI workflows are manually disabled; this is local verification.

stage: full-gate - ran (just build test lint, disk-backed temp namespace)
stage: native-validation - ran (isolated and live local app)
stage: completion-review - skipped(config: repository fast-build policy)
stage: qa - skipped(config: pipeline.qa=off; native conductor checks recorded above)
stage: plan-sync - skipped(config: planSync.enabled=false)
## Evidence
- Commits: abceba44882fac22d2795087ecbba60fac399142, 213183d2e2f5a7b8b45967c30a0a450656f9a88c, cd90a47d15d21adeebccaa94d2e4574d79d4dd01, b24b4995bb808d220e969dfd812390ef46b6c6ef, 211456dc08888ebedeaabc08e8f5c743a83d2636
- Tests: baseline: none (spec defines no Quick commands; conductor owns full gate), bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- scripts/qa/omarchy-plugin-test.sh build/qt, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- build/qt/qml/Dettivo/tests/dettivo-quick-test -input qt/qml/Dettivo/tests/qml/tst_meeting_detail_processing.qml, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- build/qt/qml/Dettivo/tests/dettivo-quick-test -input qt/qml/Dettivo/tests/qml/tst_app_states.qml, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- build/qt/qml/Dettivo/tests/dettivo-quick-test -input qt/qml/Dettivo/tests/qml/tst_app_meetings_detail.qml, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- build/qt/qml/Dettivo/tests/dettivo-quick-test -input qt/qml/Dettivo/tests/qml/tst_app_meetings.qml, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- build/qt/qml/Dettivo/tests/dettivo-quick-test -input qt/qml/Dettivo/tests/qml/tst_bar.qml, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- cargo run -q -p xtask -- lint-file-length, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- scripts/lint-omarchy-plugin.sh build/qt, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- scripts/qml-lint.sh build/qt, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- bash scripts/check-docs.sh, bwrap --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc --bind /home/gordon/.cache/dettivo-qa-tmp /tmp -- bash scripts/packaging/lint.sh, git diff --check 606c801acc1b48cbbf7ade99ee7dac90665c13f7..HEAD, Conductor full gate: just build test lint via isolated disk-backed /tmp; all Rust tests,81 Qt checks, lint/docs passed, Native title click/T, Enter/Escape and reopen persistence passed; status/footer visually checked, Installed local hotfix: actual saved-meeting analysis ready; config, title, transcript, segments, notes and both audio hashes unchanged
- PRs: