# Release gate 0.1.0: passed

Commit `bf4c3bd7a9b7` on `thor` (no compositor, x11 session, gpu vulkan), 1240882 ms; run directory `qa-evidence/run-1789376405-1090793/pack-release`. `dettivo-qa pack release` ran every step below in order and embedded each step's report in `0.1.0.json`; the claim that only external blockers remain holds when the gate passed and the unexplained list is empty.

Binaries: `dettivo` release, `dettivo-app` qt, `dettivo-osd` qt, `dettivo-qa` debug, `dettivod` release.

## Steps

| Step | Outcome | Duration | Command | Reason |
|---|---|---|---|---|
| `version_match` | pass | 1 ms | `git tag -l v0.1.0; CHANGELOG.md` | Cargo.toml 0.1.0, CHANGELOG.md has ## [0.1.0], v0.1.0 is not created yet; the release workflow tags after this gate |
| `ci_green` | skip | 1240 ms | `gh run list --commit bf4c3bd7a9b7af67aafc047a2c24afe7c04967d5 --json name,status,conclusion,workflowName,url` | no workflow run exists for bf4c3bd7a9b7: the API confirms CI workflow .github/workflows/ci.yml is disabled_manually |
| `contract_strict` | pass | 6074 ms | `dettivo-qa contract --strict` | 6 pending behind a false flag (admitted gaps: automation.macros.audit.list, automation.macros.delete, automation.macros.list, automation.macros.preview, automation.macros.run, automation.macros.upsert) |
| `mcp_harness` | pass | 0 ms | `MCP section of contract_strict/contract-replay.json` | MCP results reused from the shared contract run |
| `pack_dictation` | pass | 176707 ms | `dettivo-qa pack dictation --continue --driver atspi --out qa-evidence --timeout-ms 15000 --json --repo /home/gordon/work/dettivo-linux-wt/final-candidate` | exit 0 |
| `pack_gui` | pass | 593534 ms | `dettivo-qa pack gui --continue --driver atspi --out qa-evidence --timeout-ms 15000 --json --repo /home/gordon/work/dettivo-linux-wt/final-candidate` | exit 0 |
| `pack_meetings` | pass | 213839 ms | `dettivo-qa pack meetings --continue --driver atspi --out qa-evidence --timeout-ms 15000 --engines target/release --record --json --repo /home/gordon/work/dettivo-linux-wt/final-candidate` | exit 0 |
| `visual` | pass | 222197 ms | `dettivo-qa visual --json --repo /home/gordon/work/dettivo-linux-wt/final-candidate` | exit 0 |
| `visual_canary` | pass | 9661 ms | `dettivo-qa visual --canary --json --repo /home/gordon/work/dettivo-linux-wt/final-candidate` | exit 0 |
| `bench_report` | pass | 8 ms | `/home/gordon/work/dettivo-linux-wt/final-candidate/docs/reports/benchmarks/<date>-thor-gpu.json and <date>-<host>-cpu.json` | 2 reports: docs/reports/benchmarks/2026-09-14-thor-gpu.json (2026-09-14 on thor, dfe92a02fc8e); docs/reports/benchmarks/2026-09-14-thor-cpu.json (2026-09-14 on thor, 19613bb5be85) |
| `install_test` | pass | 0 ms | `install-test.json from /home/gordon/work/dettivo-linux-wt/final-candidate/build/install-test.json` | every one of 19 checks passed in /home/gordon/work/dettivo-linux-wt/final-candidate/build/install-test.json |
| `evidence_map` | pass | 2215 ms | `dettivo-qa evidence-map --json --repo /home/gordon/work/dettivo-linux-wt/final-candidate` | exit 0 |
| `docs_build` | pass | 3326 ms | `scripts/check-docs.sh ` | docs: ok |
| `notice_lint` | pass | 108 ms | `cargo run -q -p xtask -- lint-notice` |  |
| `real_headset` | skip | 7149 ms | `dettivo-qa audio-check --json; docs/reports/release-gate/real-headset.json` | exit 0 |

## External blockers

- `ci_green`: no workflow run exists for bf4c3bd7a9b7: the API confirms CI workflow .github/workflows/ci.yml is disabled_manually Needs the GitHub Actions workflows enabled and a run for this commit.
- `pack_dictation/hotkeys_hyprland`: not a Hyprland session (HYPRLAND_INSTANCE_SIGNATURE is unset) (driver atspi) Needs the live Hyprland desktop with the Omarchy shell (the gate runs the drives under Xvfb, off the live screen).
- `pack_gui/omarchy_bar`: not inside a Hyprland session (driver atspi) Needs the live Hyprland desktop with the Omarchy shell (the gate runs the drives under Xvfb, off the live screen).
- `pack_gui/omarchy_setup_idempotent`: not inside a Hyprland session (driver atspi) Needs the live Hyprland desktop with the Omarchy shell (the gate runs the drives under Xvfb, off the live screen).
- `pack_meetings/gpu_workload_proof`: workload attribution unavailable: engine pid 2776922 has a GPU allocation in 23 of 23 samples; process presence does not prove inference execution; diarization used CPU: no CUDA proof expected (driver none) Needs a per-process GPU workload counter that attributes inference execution to the tested engine.
- `bench_report`: docs/reports/benchmarks/2026-09-14-thor-cpu.json is the CPU row measured on this desktop under DETTIVO_FORCE_CPU=1; the CPU-only VM has not run `just bench-cpu` and `just qa-pack-meetings-cpu` Needs a CPU-only virtual machine that runs the same command and checks its report in.
- `install_test`: the report is a prefix install on this desktop; `scripts/packaging/install-test.sh --session` on a fresh CPU-only VM has not been run for this version Needs a CPU-only virtual machine that runs the same command and checks its report in.
- `real_headset`: docs/reports/release-gate/real-headset.json is missing: the virtual rig round trip ran, and a person dictates once through the real microphone on this desktop and records the receipt (version, date, device, the words, the backend) Needs a person who dictates once through the real microphone and records the receipt.

## Unexplained blockers

None: every blocker names a cause outside this machine and this repository.
